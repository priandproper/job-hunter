# Secrets (API keys) — never commit
.secrets.json

# PII — never commit. Third-party contacts, your own contact PII, private
# localStorage payload, and the generated one-off console-inject script.
data/connections.csv
data/private.local.json
data/contact.local.json
scripts/*.local.js
data/linkedin_paste.txt
data/people.local.csv
data/people.local.json
data/reachout*.md

# Logs
worker.log

__pycache__/
*.pyc
.DS_Store
data/tailored*.json
